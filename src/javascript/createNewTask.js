import { newTaskModal, newTaskOverlay } from "./config.js";

function createNewTask() {
    // newTaskModal.textContent = "TEsssssT";
    newTaskModal.classList.add("hidden");
    newTaskOverlay.classList.add("hidden");

    


    // newTaskModal.appendChild();
}
createNewTask();
export default createNewTask;
